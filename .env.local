# MongoDB Connection String
# Replace with your MongoDB URI (Atlas or local)
MONGODB_URI=mongodb://localhost:27017/vrolo-waitlist

# Or use MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/vrolo-waitlist?retryWrites=true&w=majority
